/** Re-export so server code shares the same defaults as the client. */
export { DEFAULT_SETTINGS } from '../../lib/settingsDefaults.js';
export type { DefaultSettings } from '../../lib/settingsDefaults.js';
