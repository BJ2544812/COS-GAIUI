# Production Hardening Status — Kingdom Church OS V1

**Mode:** Locked V1 pilot stabilization (no new domains)  
**Updated:** 2026-05-20

---

## Priority delivery map

| Phase | Focus | Status |
|-------|--------|--------|
| A | Production infrastructure | **Delivered** (this pass) |
| B | Backup & recovery | **Delivered** (validate script + runbooks) |
| C | UX stabilization | **Partial** (mobile nav, table wrap helper) |
| D | Mobile responsiveness | **Partial** (AppShell drawer; module-level tables ongoing) |
| E | Security hardening | **Partial** (prod headers; rate limits existing) |
| F | Operational onboarding | **Partial** (dev login hint; docs) |
| G | Observability | **Partial** (`/health/live`, `/health/ready`, structured logs exist) |
| — | Deployment automation | Existing `deploy:production`, compose templates |

---

## Phase A — Delivered artifacts

| Artifact | Purpose |
|----------|---------|
| `docker-compose.production.yml` | Healthchecks, Redis AOF, single API (workers in-process) |
| `deploy/nginx.production.conf` | `/api`, `/uploads`, `/socket.io`, `/health`, WS timeouts |
| `src/server/utils/gracefulShutdown.ts` | SIGTERM/SIGINT → HTTP close → workers → Redis → Prisma |
| `src/server/utils/workerRegistry.ts` | BullMQ clean close on restart |
| `/health/live` | Liveness probe |
| `/health/ready` | Readiness (503 if DB down) |
| Redis retry/reconnect options | `redisConnection.ts`, `eventWorker.ts` |
| `securityHeaders.ts` | Production X-Frame-Options, nosniff, etc. |

**Note:** Default API port is **4002** (align nginx upstream and `PORT` env).

---

## Phase B — Delivered artifacts

| Artifact | Purpose |
|----------|---------|
| `npm run backup:validate` | Export + schema validation |
| `RESTORE_RUNBOOK.md` | pg_dump, JSON restore, MinIO, rollback |
| `BACKUP_VALIDATION_REPORT.md` | Generated by validate script |

Restore **must** be tested manually on staging before production DR sign-off.

---

## Phase C–G — Ongoing (issue-driven)

Report friction via `STABILIZATION_BUG_LOG.md`. Do not batch-redesign modules.

Suggested next issues (when reported):

- Finance/HR table wrappers using `ResponsiveTableWrap`
- Modal max-height on small viewports
- First-run empty states in Dashboard / Settings
- CSP tightening behind env flag (optional)

---

## Validation (mandatory before merge)

```powershell
npm run lint
npm run stabilization:gate
npm run test:pilot
npm run backup:validate
```

---

## Architecture rules (unchanged)

No Prisma in controllers, no cross-module writes, no voucher bypass, tenant isolation preserved, workflow engine ownership unchanged.
